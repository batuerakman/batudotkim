import 'package:flutter/material.dart';

dynamic buttonPressLabel = TextStyle(
    fontFamily: 'SegoeUI',
    fontSize: 24,
    color: Colors.white,
    shadows: [Shadow(offset: const Offset(1, 0), blurRadius: 2, color: Colors.black.withAlpha(120))]);
dynamic userRowLabel = const TextStyle(fontFamily: 'SegoeUI', fontSize: 20, color: Colors.white);
dynamic menuLabel = TextStyle(fontFamily: 'SegoeUI', fontSize: 36, fontWeight: FontWeight.w300, color: Colors.white.withAlpha(120));
dynamic menuLabelSelected = const TextStyle(fontFamily: 'SegoeUI', fontSize: 36, color: Colors.white);
dynamic itemLabel = const TextStyle(fontFamily: 'SegoeUI', fontSize: 30, color: Colors.white);
dynamic itemLabelLight = const TextStyle(fontFamily: 'SegoeUI', fontSize: 30, color: Colors.white, fontWeight: FontWeight.w300);

const Color tileColor = Color(0xff009600);
